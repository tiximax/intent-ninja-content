# Production ENV template for Vite app
# Copy this file to your hosting provider/CI environment variables (do NOT commit real secrets)
# Notes:
# - MODE is handled by Vite build; do not set here.
# - Fail-fast validation will require Supabase URL/KEY when mock/bypass are disabled.

# Core runtime toggles
VITE_USE_MOCK_CONTENT=false
VITE_BYPASS_AUTH=false
VITE_E2E_TEST_MODE=false

# Supabase (required when mock content disabled OR auth bypass disabled)
# Replace placeholders with your project's values (store in hosting/CI secrets)
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Optional providers
# Set to true only if you have corresponding edge functions/providers configured
VITE_ENABLE_SERPAPI_PROVIDER=false

# Observability (optional but recommended)
# Sentry: DSN to enable error tracking in production
VITE_SENTRY_DSN=
VITE_SENTRY_ENV=production

# Network fetch logger (breadcrumbs)
# true to enable; false to disable
VITE_ENABLE_FETCH_LOGGER=true
# Whitelist additional origins to be logged (comma-separated)
VITE_LOG_ORIGINS=
